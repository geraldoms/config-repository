server:
  port: 8283
  servlet:
    context-path: /api

spring:
  application:
    name: rating-app

eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 5

management:
  security:
    sessions: never